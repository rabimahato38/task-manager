[pytest]
minversion = 7.0
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov=.
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
